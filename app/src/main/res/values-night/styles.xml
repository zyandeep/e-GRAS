<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <item name="dialogSheetAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/open_sans</item>
    </style>

    <!-- Spot dialog theme -->
    <style name="mySpotDialogTheme" parent="android:Theme.Holo.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Medium</item>
        <item name="DialogSpotCount">5</item>
        <item name="DialogTitleText">@string/spot_dialog_message</item>
        <item name="DialogSpotColor">@color/colorAccent</item>
    </style>

</resources>
